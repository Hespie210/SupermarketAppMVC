<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="/css/theme.css">
  <title>Hespie Supermarket</title>
</head>
<body>
    <%- include('partials/header') %>


    <div class="container">
        <div class="page-hero text-center my-5">
          <h1 class="page-title mb-2">Welcome to Hespie Supermarket</h1>
          <p class="page-subtitle">Fresh picks, quick checkout, and a seamless cart and wishlist.</p>

          <% if (!user) { %>
            <div class="d-flex justify-content-center gap-3 mt-3">
              <a class="btn btn-primary" href="/register">Register</a>
              <a class="btn btn-secondary" href="/login">Login</a>
            </div>
          <% } else { %>
            <div class="d-flex justify-content-center gap-3 mt-3">
              <% if (user.role === "admin") { %>
                <a class="btn btn-primary" href="/inventory">Go to Inventory</a>
              <% } %>
              <a class="btn btn-secondary" href="/shopping">Shop Now</a>
            </div>
          <% } %>
        </div>
        
    </div>
</body>
</html>
