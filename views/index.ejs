<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&family=Poppins:wght@500;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="/css/theme.css">
  <title>Hespie Supermarket</title>
  <style>
    body {
      font-family: 'Manrope', sans-serif;
      background: linear-gradient(135deg, #fff6e8 0%, #f3f8ff 50%, #e8fff6 100%);
      color: #0f172a;
    }
    .page-hero {
      position: relative;
      overflow: hidden;
      border-radius: 24px;
      padding: 64px 48px;
      background: radial-gradient(circle at 20% 20%, rgba(255, 255, 255, 0.35), transparent 35%),
                  radial-gradient(circle at 80% 10%, rgba(255, 255, 255, 0.4), transparent 30%),
                  linear-gradient(120deg, #ff8f70 0%, #ff6a95 30%, #3acfd5 100%);
      color: #0f172a;
      box-shadow: 0 20px 70px rgba(255, 122, 122, 0.3);
    }
    .page-hero .badge {
      background: rgba(255, 255, 255, 0.2);
      color: #0f172a;
      backdrop-filter: blur(4px);
      border: 1px solid rgba(255, 255, 255, 0.4);
    }
    .page-title {
      font-family: 'Poppins', sans-serif;
      font-size: clamp(2rem, 3vw, 2.75rem);
      font-weight: 700;
      color: #0f172a;
      margin-bottom: 12px;
    }
    .page-subtitle {
      font-size: 1.05rem;
      max-width: 680px;
      margin: 0 auto 20px;
      color: #0f172a;
      opacity: 0.92;
    }
    .btn-primary {
      background: #0f172a;
      border: none;
      box-shadow: 0 12px 32px rgba(15, 23, 42, 0.3);
    }
    .btn-secondary {
      background: #fef08a;
      color: #0f172a;
      border: none;
      box-shadow: 0 12px 32px rgba(254, 240, 138, 0.5);
      font-weight: 700;
    }
    .feature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 18px;
      margin-top: 32px;
    }
    .feature-card {
      border: none;
      border-radius: 18px;
      padding: 18px 16px;
      background: #ffffff;
      box-shadow: 0 12px 50px rgba(15, 23, 42, 0.08);
      min-height: 150px;
    }
    .feature-icon {
      width: 42px;
      height: 42px;
      border-radius: 12px;
      display: grid;
      place-items: center;
      color: #0f172a;
      font-weight: 700;
      margin-bottom: 10px;
    }
    .icon-orange { background: #ffd8c2; }
    .icon-yellow { background: #fff3a3; }
    .icon-teal { background: #c8fff1; }
    .icon-blue { background: #cbe5ff; }
    .pill {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      border-radius: 999px;
      padding: 10px 16px;
      background: rgba(255, 255, 255, 0.85);
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
      color: #0f172a;
    }
    .pill span {
      font-weight: 700;
      color: #0f172a;
    }
  </style>
</head>
<body>
    <%- include('partials/header') %>


    <div class="container py-5">
      <div class="page-hero text-center">
        <div class="d-inline-flex align-items-center gap-2 mb-3 pill">
          <span>Fresh • Fast • Friendly</span>
          <small class="text-muted">Every cart gets a boost today</small>
        </div>
        <h1 class="page-title">Welcome to Hespie Supermarket</h1>
        <p class="page-subtitle">Bright produce, bold flavors, and breezy checkout. Build your basket with handpicked essentials and get them quicker than ever.</p>

        <div class="d-flex flex-wrap justify-content-center gap-3 mt-3">
          <% if (!user) { %>
            <a class="btn btn-secondary px-4 py-2" href="/register">Create Account</a>
            <a class="btn btn-primary px-4 py-2" href="/login">Login &amp; Shop</a>
          <% } else { %>
            <% if (user.role === "admin") { %>
              <a class="btn btn-secondary px-4 py-2" href="/inventory">Manage Inventory</a>
            <% } %>
            <a class="btn btn-primary px-4 py-2" href="/shopping">Shop Now</a>
          <% } %>
        </div>
      </div>

      <div class="feature-grid">
        <div class="feature-card">
          <div class="feature-icon icon-orange">⚡</div>
          <h5 class="fw-bold mb-1">Lightning Checkout</h5>
          <p class="text-muted mb-0">Streamlined cart-to-payment flow so you spend less time waiting and more time enjoying.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon icon-teal">🥑</div>
          <h5 class="fw-bold mb-1">Freshly Picked</h5>
          <p class="text-muted mb-0">Daily restocks with bright, crisp produce and pantry staples curated for quality.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon icon-yellow">💛</div>
          <h5 class="fw-bold mb-1">Wishlist Magic</h5>
          <p class="text-muted mb-0">Save favorites, track price drops, and add to cart with a single tap.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon icon-blue">🚚</div>
          <h5 class="fw-bold mb-1">Pickup &amp; Delivery</h5>
          <p class="text-muted mb-0">Choose your pace: rapid pickup or doorstep delivery, both with live status.</p>
        </div>
      </div>
    </div>
</body>
</html>
